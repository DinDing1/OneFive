# 文件管理模块实现计划

## Summary

基于 P115Client SDK 实现 115 云盘文件管理模块。在设置页面添加 Open API 开关和 AppID 选择，启用后优先使用 Open API（大分页），Web API 作为回退。**此模式作为后续模块的标准模式。**

## Current State Analysis

- **前端 `Files.vue`**：占位页面
- **后端**：无文件管理相关 API 和 Service
- **P115Client**：已安装，目前仅用 `user_my()`
- **架构模式**：API 路由 → Service 单例 → 三层架构

## 可选 AppID 列表

| AppID | 名称 |
|-------|------|
| 100195137 | VidHub |
| 100195793 | Senplayer |
| 100195145 | Filebar |
| 100196987 | CloudDrive2 |
| 100195271 | 飞牛私有云 |
| 100195135 | 网易爆米花 |
| 100195199 | 极空间 |

## Proposed Changes

### 1. 后端：新建 `services/token_service.py`（Token 管理服务）

**职责**：管理 Open API 的 access_token，自动获取和刷新。

**核心方法**：
- `get_access_token()` — 获取有效 token，过期自动刷新
- `_refresh_token()` — 用 refresh_token 刷新
- `_authorize_by_cookie()` — 用 Cookie 模拟扫码获取 token
- `is_open_api_enabled()` — 检查 Open API 是否启用

**Token 获取流程（Cookie 模拟扫码）**：
1. 调用 `/open/authDeviceCode` 获取 uid + code_verifier
2. Cookie 请求 `/api/2.0/prompt.php?uid=xxx` 自动扫描
3. Cookie 请求 `/api/2.0/slogin.php?key=xxx&uid=xxx` 确认
4. 调用 `/open/deviceCodeToToken` 换取 access_token + refresh_token

**存储**（setting 表）：
- `open_api_enabled` — 是否启用 Open API（"1" / "0"）
- `open_app_id` — 选择的 AppID
- `open_access_token` — access_token
- `open_refresh_token` — refresh_token
- `open_token_expire` — 过期时间戳

### 2. 后端：新建 `services/file_service.py`（文件管理服务）

**核心方法**：
- `list_files(cid, limit, offset)` — 列出目录
- `get_file_info(file_id)` — 获取详情
- `create_folder(name, pid)` — 创建目录
- `search_files(keyword, cid)` — 搜索

**API 选择逻辑**：
```
if Open API 启用 且 token 有效:
    用 P115OpenClient.fs_files_open()（分页上限 7000+）
else:
    用 P115Client.fs_files()（分页上限 1150）
```

### 3. 后端：新建 `api/files.py`（文件管理 API 路由）

路由前缀 `/api/files`：

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/list` | 列出目录，参数：`cid`、`limit`、`offset` |
| GET | `/info/{file_id}` | 获取详情 |
| POST | `/mkdir` | 创建目录 |
| GET | `/search` | 搜索 |

### 4. 后端：更新 `main.py`

注册 `files_router`。

### 5. 后端：更新 `logger.py` MODULE_NAMES

```python
"onefive.services.file_service": "File",
"onefive.services.token_service": "Token",
"onefive.api.files": "API",
```

### 6. 前端：新建 `api/files.ts`

定义 FileItem、FileListResponse 接口和 filesApi 对象。

### 7. 前端：重写 `views/Files.vue`

**布局**：
- 面包屑导航（根目录 > 文件夹A > 文件夹B）
- 工具栏：返回上级、刷新、新建文件夹
- 文件/目录列表

**交互**：
- 点击文件夹 → 进入目录，更新面包屑
- 点击面包屑 → 跳转对应层级
- 返回上级 → 回到父目录

**样式**：macOS 风格，响应式适配

### 8. 前端：更新 `views/Settings.vue`

在"应用信息"卡片下方新增 **115 开放平台** 卡片：

```
┌─────────────────────────────────┐
│ 115 开放平台                     │
├─────────────────────────────────┤
│ 启用 Open API        [开关]     │
│ AppID                [下拉选择]  │
│ Token 状态           已获取/未获取 │
└─────────────────────────────────┘
```

- 开关控制 `open_api_enabled`
- 下拉选择 AppID（7 个选项）
- Token 状态显示当前 token 是否有效
- 切换开关或选择 AppID 时自动触发 token 获取

### 9. 前端：更新 `Home.vue`

文件管理卡片去掉 `disabled` 状态。

## 标准模式（后续模块遵循）

```
设置页面：Open API 开关 + AppID 选择
    ↓
token_service：自动管理 token
    ↓
业务 service：Open API 优先，Web API 回退
    ↓
API 路由：统一响应格式
```

## Verification Steps

1. 设置页面显示 Open API 开关和 AppID 选择
2. 启用 Open API 后，token 自动获取成功
3. `GET /api/files/list?cid=0` 返回根目录文件列表
4. 点击文件夹进入子目录，面包屑更新
5. 禁用 Open API 后，仍可用 Web API 正常浏览
